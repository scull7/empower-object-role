// Generated by CoffeeScript 1.10.0
(function() {
  var UrlObject, _objectFoldp, getObjectsFromUrl, isValidName, isValidValue, tokenizeUrl;

  isValidName = function(str) {
    switch (false) {
      case !!isNaN(parseInt(str, 10)):
        return false;
      case str !== null:
        return false;
      case str !== void 0:
        return false;
      case !(str.length < 1):
        return false;
      default:
        return true;
    }
  };

  isValidValue = function(val) {
    switch (false) {
      case val !== null:
        return false;
      case val !== void 0:
        return false;
      case !((typeof val) === 'string' && val.length < 1):
        return false;
      default:
        return true;
    }
  };

  UrlObject = function(name, id) {
    return {
      name: name,
      id: id
    };
  };

  tokenizeUrl = function(url) {
    return url.split('/');
  };

  _objectFoldp = function(acc, cur, index, arr) {
    if ((isValidName(cur)) && (isValidValue(arr[index + 1]))) {
      return acc.concat(UrlObject(cur, arr[index + 1]));
    } else {
      return acc;
    }
  };

  getObjectsFromUrl = function(url) {
    return (tokenizeUrl(url)).reduce(_objectFoldp, []);
  };

  module.exports = {
    UrlObject: UrlObject,
    isValidName: isValidName,
    isValidValue: isValidValue,
    tokenizeUrl: tokenizeUrl,
    getObjectsFromUrl: getObjectsFromUrl
  };

}).call(this);
